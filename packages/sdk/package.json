{
  "name": "@paykit/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "XRPL PayKit SDK — types, webhook signing/verification, and a thin client.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0"
  }
}
